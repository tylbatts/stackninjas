// Removed unused default React import under new JSX transform

export default function Profile() {
  return (
    <div>
      <h1 className="text-2xl font-semibold mb-4">Profile</h1>
      <p>Admin profile & settings will go here.</p>
    </div>
  );
}